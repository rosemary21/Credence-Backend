import type { Settlement } from './repositories/settlementsRepository.js'

/**
 * Transaction entity maps to the Settlement record.
 * This represents a processed payout or payment event.
 */
export type Transaction = Settlement
