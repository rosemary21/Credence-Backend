# Secret placeholder for sensitive configuration.
# IMPORTANT: Do NOT commit real secrets to version control.
# In production, populate via:
#   - kubectl create secret generic credence-backend-secret \
#       --from-literal=DATABASE_PASSWORD=<real-password> \
#       --from-literal=API_KEY=<real-key> \
#       -n credence
#   - Or use a secrets manager (Vault, AWS Secrets Manager, etc.)
#
# The base64 values below are placeholders ("CHANGEME" / "changeme-api-key").
apiVersion: v1
kind: Secret
metadata:
  name: credence-backend-secret
  namespace: credence
  labels:
    app.kubernetes.io/name: credence-backend
    app.kubernetes.io/component: secret
type: Opaque
data:
  # echo -n "CHANGEME" | base64
  DATABASE_PASSWORD: Q0hBTkdFTUU=
  # echo -n "changeme-api-key" | base64
  API_KEY: Y2hhbmdlbWUtYXBpLWtleQ==
